# 2026-07-01 — GDD 拆分 + 水域 + 多生成器 + ADR

## 完成

### GDD 拆分
- 单个 `docs/gdd.md` (227行) → `docs/gdd/` 子文件夹 (10 文件)
- 每个模块独立可编辑可讨论
- 主 `gdd.md` 变为入口指针

### 新设计内容
- **水域回归**：8% 比例，不可通行，产弹道。环面+水域=天然隘口
- **6 种地图生成器**：balanced / symmetric / fertile / harsh / mountain_pass / archipelago
- 生成器独立模块——种子+生成器ID=完全可复现

### ADR
- 4 篇初始 ADR：环面/无单位上限/固定伤害/不做经验

### 其他
- 战斗公式校准目标值表（标记了需要原型时调整的组合）
- 科技树信息博弈细化（可见/不可见/可推断三类）

## 接下来
Python 原型可以启动了。需要先决定：
1. 用什么结构 start（`prototype/` 还是直接在根目录写？）
2. 从哪个模块开始（map 生成器最独立 → 先做）
